[project]
name = "pdfsplitter-cli"
version = "1.0.0"
description = "Standalone PDF splitter"
requires-python = ">=3.8"
dependencies = ["pypdf"]

[tool.poetry]
name = "pdfsplitter-cli"
version = "1.0.0"
