[project]
name = "pdfsplitter-cli"
version = "0.1.0"
description = "Standalone PDF splitter extracted from a monorepo"
requires-python = ">=3.8"
dependencies = ["pypdf"]

[tool.poetry]
name = "pdfsplitter-cli"
version = "0.1.0"
